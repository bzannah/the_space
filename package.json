{
    "devDependencies": {
        "@symfony/webpack-encore": "^0.17.0"
    },
    "license": "UNLICENSED",
    "private": true,
    "scripts": {
        "dev-server": "bin/console server:run localhost:8080",
        "dev": "encore dev",
        "watch": "encore dev --watch",
        "build": "encore production"
    }
}
